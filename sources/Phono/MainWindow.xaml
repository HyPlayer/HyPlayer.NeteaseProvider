<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Phono.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="using:Phono.Views.App"
    xmlns:base="using:Richasy.WinUIKernel.Share.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Phono"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Phono"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <base:AppTitleBar
            x:Name="TitleBar"
            Title="Phono"
            Height="48"
            VerticalAlignment="Top"
            Canvas.ZIndex="99"
            IsBackButtonVisible="True"
            IsBackEnabled="{x:Bind _navigationService.CanGoBack, Mode=OneWay}"
            Subtitle="Preview">
            <base:AppTitleBar.Content>
                <AutoSuggestBox
                    Width="384"
                    Height="36"
                    PlaceholderText="Search something here..."
                    QueryIcon="Zoom" />
            </base:AppTitleBar.Content>
            <base:AppTitleBar.Footer>
                <Button Background="Transparent" BorderThickness="0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <PersonPicture Width="30" Height="30" />
                        <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" VerticalAlignment="Center">
                            Jeff Zeng
                        </TextBlock>
                    </StackPanel>
                </Button>
            </base:AppTitleBar.Footer>
            <base:AppTitleBar.IconElement>
                <BitmapIcon UriSource="/Assets/StoreLogo.png"/>
            </base:AppTitleBar.IconElement>
        </base:AppTitleBar>
        <app:RootPage x:Name="RootPageHost" Grid.Row="1" />
    </Grid>
</Window>
